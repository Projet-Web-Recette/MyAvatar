{% extends 'base.html.twig' %}

{% block page_title %}Mon profil{% endblock %}

{% block page_content %}
    <div class="section flex flex-row justify-center items-center space-x-24">
        <div class="flex flex-col justify-center items-center gap-2 space-y-8 h-72">
            <span class="flex items-center text-6xl font-bold text-center bungee-font h-full select-none">Actions</span>
            <div class="grid grid-cols-2 gap-8">
                <a href="{{ path('edit_pp') }}"
                   class="p-6 rounded-xl text-center border-4 border-black-main black-shadow-box hover:bg-gray-300 transition-all duration-200">
                    <h2 class="text-4xl font-bold">&#128221; Modifier l'image de profil</h2>
                </a>
                <a href="{{ path('edit_password') }}"
                   class="p-6 rounded-2xl text-center border-4 border-black-main black-shadow-box hover:bg-gray-300 transition-all duration-200">
                    <h2 class="text-4xl font-bold">&#128275; Modifier le mot de passe</h2>
                </a>
            </div>
            <a href="{{ path('logout') }}"
               class="p-6 rounded-2xl text-center border-4 border-black-main black-shadow-box hover:bg-gray-300 transition-all duration-200">
                <h2 class="text-4xl font-bold">&#128682; Se déconnecter</h2>
            </a>
        </div>
    </div>
{% endblock %}